services:
  - type: web
    name: cancan-online-ws
    runtime: node
    rootDir: online-server
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 20
